# create an RSL Reactive States Language, based on the AWS Step Functions Language ASL that describes workflows with state machines

# RSL = ASL topology + Rxjs execution policies
## A declarative language describing how values move through a reactive workflow over time.

